# Day Mapping — Deploy and manage Azure compute resources

| Day | Topics exercised | Build artifact |
|---|---|---|
| Day-007 | App Service application settings configuration; `Anthropic__EnablePromptCaching` operational toggle; ARM PATCH for settings management; `WEBSITE_RUN_FROM_PACKAGE=1` for Kudu zip deploy; double-underscore → colon IConfiguration binding | Deployed `app-ai-lab-api-dev-eastus-gio` with 6 App Service settings via ARM PATCH (PUT blocked by TLS inspection proxy) |
